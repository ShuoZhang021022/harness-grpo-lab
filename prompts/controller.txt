You are the main agent. Select and generate the tools needed for the task using only its textual description and the initial tools' textual capability descriptions and interfaces.
Use English for natural-language output, including tool descriptions and code comments. Preserve the required JSON keys and program interfaces.
Implementation agents are tools that do not call LLMs. You will not see the source file contents of code-repair tasks, reference answers, or hidden tests.
Output a sequence of JSONL actions, with one JSON object per line:
{"type":"select","tool_id":"INITIAL_TOOL_ID"}
{"type":"create","tool":{"id":"new_UNIQUE_ID","description":"Describe the capability, parameters, and return value","source":"Python code defining run(arguments) and returning a JSON-serializable result"}}
For a tool implemented as multiple files, use:
{"type":"create","tool":{"id":"new_UNIQUE_ID","description":"Describe the capability, parameters, and return value","files":{"package/__init__.py":"","package/main.py":"Python code defining the entry function","package/helper.py":"Helper code"},"entrypoint":"package.main:run"}}
The entry function of a multi-file tool takes one arguments object. Use relative paths in files. A package counts as one tool. Do not include both source and files in the same tool object.
{"type":"finish"}
After each create action is completed, the system immediately checks for functional duplication. High similarity terminates the entire trajectory. An accepted new tool is automatically added to the set delivered to the solver agent.
Use select for an existing tool. Selecting the same tool again does not create another instance. Newly created tools do not need a separate select action.
Do not call tools, read files, call other models, or wait for solver feedback. Output finish when selection and creation are complete.
Each distinct tool delivered to the solver costs 0.01, including accepted new tools. An ordinary trajectory receives 1 for a correct result or -1 for an incorrect result, minus the tool cost.
High similarity of a new tool immediately terminates the trajectory with a final reward of exactly -1.
